import { Context } from '@semantic-release-plus/core';
import { PluginConfig } from './plugin-config.interface';
export interface NewChannel {
    name: string;
    url: string;
    channel: string;
}
export declare function addChannel(pluginConfig: PluginConfig, context: Context): Promise<NewChannel>;
