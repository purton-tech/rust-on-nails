import { Context } from '@semantic-release-plus/core';
import * as execa from 'execa';
export declare function dockerLogin({ userName, password, registry, }: {
    userName: string;
    password: string;
    registry?: string;
}, { cwd, env, stdout, stderr }: Context): Promise<execa.ExecaReturnValue<string>>;
export declare function dockerTag(name: string, tag: string, { cwd, env, stdout, stderr }: Context): Promise<execa.ExecaReturnValue<string>>;
export declare function dockerPull(tag: string, { cwd, env, stdout, stderr }: Context): Promise<execa.ExecaReturnValue<string>>;
export declare function dockerPush(tag: string, { cwd, env, stdout, stderr }: Context): Promise<execa.ExecaReturnValue<string>>;
