"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.normalizeConfig = void 0;
const image_name_1 = require("./image-name");
function normalizeConfig(pluginConfig) {
    const { name, registry, skipLogin, publishChannelTag } = {
        registry: '',
        skipLogin: false,
        publishChannelTag: true,
        ...pluginConfig,
    };
    const imageName = new image_name_1.ImageName(name);
    if (!imageName.registry && registry) {
        imageName.registry = registry;
    }
    return {
        image: imageName,
        skipLogin,
        publishChannelTag,
    };
}
exports.normalizeConfig = normalizeConfig;
//# sourceMappingURL=normalize-config.js.map